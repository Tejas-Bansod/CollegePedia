<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      background-color: #F3E8FF; /* Light purple background */
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      margin: 0;
    }
    .header {
      background-color: #6B46C1; /* Primary purple for header */
      color: #FFFFFF; /* White text */
      padding: 1rem 2rem;
      box-shadow: 0 4px 6px -1px rgba(107, 70, 193, 0.1);
    }
    .error-container {
      background-color: #FFFFFF; /* White for error container */
      border-radius: 0.5rem;
      box-shadow: 0 4px 6px -1px rgba(107, 70, 193, 0.1); /* Purple-tinted shadow */
      padding: 2rem;
      max-width: 500px;
      width: 100%;
      margin: auto;
      text-align: center;
    }
    .error-title {
      color: #9F1239; /* Dark red for error title */
      font-weight: 700;
    }
    .error-message {
      color: #6B46C1; /* Purple for message text */
      font-size: 1.125rem;
    }
    .btn-primary {
      background-color: #6B46C1; /* Purple for buttons */
      border-color: #6B46C1;
      padding: 0.75rem 1.5rem;
      font-weight: 600;
      color: #FFFFFF;
      border-radius: 0.375rem;
      text-decoration: none;
      display: inline-block;
    }
    .btn-primary:hover {
      background-color: #9F7AEA; /* Light purple on hover */
      border-color: #9F7AEA;
      color: #FFFFFF;
      text-decoration: none;
    }
    @media (max-width: 640px) {
      .error-container {
        padding: 1.5rem;
      }
      .header {
        padding: 1rem;
      }
    }
  </style>
</head>
<body>
  <!-- Header -->
  <header class="header">
    <div class="container mx-auto flex justify-between items-center">
      <h1 class="text-xl font-bold">Error</h1>
      <a href="/" class="text-white hover:text-purple-200">
        <i class="fas fa-home mr-1"></i> Back to Home
      </a>
    </div>
  </header>

  <!-- Main Content -->
  <div class="flex-grow flex items-center justify-center p-4">
    <div class="error-container">
      <h2 class="text-2xl error-title mb-4">Error</h2>
      <p class="error-message mb-4">Something went wrong. Please try again later.</p>
      <a href="/" class="btn btn-primary">Go back to home</a>
    </div>
  </div>

  <!-- Bootstrap JS and Popper.js -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>